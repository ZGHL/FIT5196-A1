# AI records required before submission

This assignment used conversational AI. Before creating the final Moodle ZIP:

1. Export every complete assignment-related conversation without shortening it. Preserve the original-language record; where an English version is required, identify it as a translation rather than replacing the original evidence.
2. Create an English `Group030_AI_index.pdf` using the declaration template's fields: member/tool/model, complete export filename, purpose, affected files/sections, and independent verification.
3. Place the index and complete export(s) in this directory.
4. Have all five members complete and sign `Group030_AI_declaration.pdf`.

The five registered conversation files are `AI-01.pdf` through `AI-05.pdf`. Each member must confirm that their file is a complete, unshortened record of their own assignment-related conversation before signing the declaration.

Independent checks available for the index include: `TXT-01`–`TXT-18` public cases (18/18 PASS), student-designed tests (8/8 PASS), the `VAL-*` register (64/64 PASS), join-cardinality assertions, and a clean-directory Restart & Run All of both self-contained notebooks.
