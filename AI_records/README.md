# AI records required before submission

This assignment used conversational AI. Before creating the final Moodle ZIP:

1. Export every complete assignment-related conversation without shortening it. Preserve each conversation in its original language.
2. Create an English `Group030_AI_index.pdf` using the declaration template's fields: member/tool/model, complete export filename, purpose, affected files/sections, and independent verification.
3. Place the index and complete export(s) in this directory.
4. Keep the signed `Group030_AI_declaration.pdf` at the project root so it is placed at the root of the final submission ZIP.

The six registered conversation files are `AI-01.pdf` through `AI-05.pdf` and `AI-06.html`. Each member must confirm that the records attributed to them are complete, unshortened exports of their assignment-related conversations.

Independent checks available for the index include: `TXT-01`–`TXT-18` public cases (18/18 PASS), student-designed tests (22/22 PASS), the `VAL-*` register (68/68 PASS), join-cardinality assertions, and a clean-directory Restart & Run All of both self-contained notebooks.
